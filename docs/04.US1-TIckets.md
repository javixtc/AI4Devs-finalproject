# US1  Google OAuth Authentication: Work Tickets

**New Bounded Context**: identity
**Migrated BCs**: meditationbuilder · meditation.generation · playback
**Total tasks**: 16 (new BC + migration of 3 existing BCs)
**Status**: Completed

---

## AUTH-T001  BDD: Google OAuth authentication feature

**Type**: Story · **Layer**: BDD First
**Status**: Completed

**Description**
Create eatures/identity/US1.feature with the 5 spec scenarios (first access, returning user, protected route, logout, cancellation) + step definitions in PENDING state.

**Artifacts**
- ackend/src/test/resources/features/identity/US1.feature
- ackend/src/test/java/.../identity/bdd/steps/

**Acceptance Criteria**
- mvn test -Dcucumber.filter.tags=@identity runs without UNDEFINED steps
- 5 scenarios defined in pure Gherkin (no technical language)

---

## AUTH-T002  API First: OpenAPI identity US1

**Type**: Task · **Layer**: API First
**Status**: Completed

**Description**
Define openapi/identity/US1.yaml with the 4 backend capabilities: C1 (authenticate with Google), C2 (recognize existing user  same JWT), C3 (protected routes  401), C4 (logout).

**Artifacts**
- ackend/src/main/resources/openapi/identity/US1.yaml

**Acceptance Criteria**
- Redocly/Spectral lint passes with no errors
- No paths outside C1C4
- Main endpoint: POST /api/v1/identity/auth/google

---

## AUTH-T003  Domain: PerfilDeUsuario Aggregate

**Type**: Task · **Layer**: Domain
**Status**: Completed

**Description**
Implement aggregate root PerfilDeUsuario as a Java record (id UUID, googleIdentifier, email, name, pictureUrl, createdAt with injected Clock) + invariants (uniqueness by googleIdentifier, immutability) + in/out ports.

**Ports**
- In: AutenticarConGooglePort, CerrarSesionPort
- Out: BuscarPerfilPorGoogleIdPort, PersistirPerfilPort, ValidarCredencialGooglePort, EmitirTokenSesionPort

**Artifacts**
- ackend/src/main/java/.../identity/domain/

**Acceptance Criteria**
- Unit tests pass
- No Spring or infrastructure imports in the domain

---

## AUTH-T004  Application: Authentication Use Cases

**Type**: Task · **Layer**: Application
**Status**: Completed

**Description**
Implement IniciarSesionConGoogleUseCase (validates credential  finds or creates PerfilDeUsuario  issues session token) and CerrarSesionUseCase (invalidates token). Pure orchestration via output ports.

**Artifacts**
- ackend/src/main/java/.../identity/application/

**Acceptance Criteria**
- Unit tests with mocks for all output ports
- No business logic in this layer (orchestration only)

---

## AUTH-T005  Infra: BC identity output adapters

**Type**: Task · **Layer**: Infrastructure
**Status**: Completed

**Description**
Output adapters for BC identity: persistence of PerfilDeUsuario in identity.users table (PostgreSQL), validation of the Google id_token via JWKS (Spring Security OAuth2 Resource Server), issuance of the own session JWT (key configurable via env).

**Artifacts**
- ackend/src/main/java/.../identity/infrastructure/out/
- Flyway migration: V_identity_users.sql

**Acceptance Criteria**
- Integration tests for persistence adapter with Testcontainers pass
- Google adapter tested with simulated service (WireMock)

---

## AUTH-T006  Shared Infra: Cross-cutting security filter

**Type**: Task · **Layer**: Infrastructure Shared · **Parallelizable**: Yes
**Status**: Completed

**Description**
Implement cross-cutting security filter in shared/security/ that extracts userId from the own session JWT and injects it into the SecurityContext. Replaces the X-User-Id header mechanism in controllers of the 3 existing BCs.

**Artifacts**
- ackend/src/main/java/.../shared/security/BearerTokenFilter.java

**Acceptance Criteria**
- Filter tested with valid, invalid, and absent tokens
- Does not modify domain or application of existing BCs

---

## AUTH-T007  Controllers: AuthController + BDD green

**Type**: Task · **Layer**: Controllers
**Status**: Completed

**Description**
Implement AuthController in identity/infrastructure/in/rest/ (C1/C2 authentication endpoint + C4 logout). Complete BDD step definitions so all 5 scenarios turn GREEN.

**Artifacts**
- ackend/src/main/java/.../identity/infrastructure/in/rest/AuthController.java

**Acceptance Criteria**
- BDD tag @identity green (5 scenarios)
- Contract complies with US1.yaml
- No business logic in the controller

---

## AUTH-T008  Controller Migration: X-User-Id  SecurityContext

**Type**: Task · **Layer**: Controllers · **Parallelizable**: Yes
**Status**: Completed

**Description**
In the controllers of meditationbuilder, meditation.generation and playback: replace reading the X-User-Id header with extracting userId from the SecurityContext.

**Artifacts**
- ackend/src/main/java/.../meditationbuilder/infrastructure/in/rest/
- ackend/src/main/java/.../meditation/generation/infrastructure/in/rest/
- ackend/src/main/java/.../playback/infrastructure/in/rest/

**Acceptance Criteria**
- Tests for all 3 existing BCs pass
- X-User-Id header removed from the entire input layer

---

## AUTH-T008b  BDD Test Infrastructure Migration

**Type**: Task · **Layer**: BDD Tests · **Parallelizable**: Yes
**Status**: Completed

**Description**
Update the 3 *CucumberSpringConfiguration and their step definitions to use JWT Bearer instead of X-User-Id. Add @MockBean ValidarCredencialGooglePort, obtain JWT via POST /v1/identity/auth/google in @Before, remove obsolete headers.

**Artifacts**
- ackend/src/test/java/.../bdd/ (all bounded contexts)

**Acceptance Criteria**
- 18 BDD scenarios pass
- X-Test-User-Id and X-User-ID removed from all step definitions

---

## AUTH-T009  Frontend: Auth state + LoginPage

**Type**: Task · **Layer**: Frontend
**Status**: Completed

**Description**
Implement uthStore (Zustand) with token, name and picture of the user + LoginPage with the @react-oauth/google button + protected routes via AuthGuard that redirects to /login without an active session.

**Artifacts**
- rontend/src/state/authStore.ts
- rontend/src/pages/LoginPage.tsx
- rontend/src/components/AuthGuard.tsx

**Acceptance Criteria**
- LoginPage renders without a session
- Redirect to /login when accessing /library without a token

---

## AUTH-T010  Frontend: identity API client + header

**Type**: Task · **Layer**: Frontend · **Parallelizable**: Yes
**Status**: Completed

**Description**
Generate OpenAPI client for identity (
pm run generate:api) + identity-client.ts wrapper for C1 and C4 + update all API clients to send Authorization: Bearer <token> + header with name, picture and logout link.

**Artifacts**
- rontend/src/api/generated/identity/
- rontend/src/api/identity-client.ts

**Acceptance Criteria**
- 
pm run generate:api runs without errors
- X-User-Id header removed from all frontend clients
- Header renders profile data

---

## AUTH-T011  Contract Tests: identity OpenAPI

**Type**: Task · **Layer**: Contracts · **Parallelizable**: Yes
**Status**: Completed

**Description**
Provider/consumer tests against openapi/identity/US1.yaml: verify that AuthController honors the contract exactly (C1, C2, C4) and that the shared/security filter provides the correct userId to the other BCs.

**Artifacts**
- ackend/src/test/contracts/

**Acceptance Criteria**
- All contract tests pass
- No deviation from the OpenAPI contract

---

## AUTH-T012  Backend E2E: Full authentication flow

**Type**: Task · **Layer**: Backend E2E
**Status**: Completed

**Description**
Full flow with simulated Google credential: authentication  JWT  access to protected resource. Logout flow  access attempt rejected (401).

**Artifacts**
- ackend/src/test/java/.../identity/e2e/GoogleAuthE2ETest.java

**Acceptance Criteria**
- All backend E2E scenarios pass (4/4)

---

## AUTH-T012b  Backend E2E Migration: 3 existing BCs

**Type**: Task · **Layer**: Backend E2E · **Parallelizable**: Yes
**Status**: Completed

**Description**
Update ManualCompositionE2ETest, AiGenerationE2ETest, GenerateMeditationE2ETest to use JWT Bearer instead of the obsolete X-Test-User-Id header.

**Acceptance Criteria**
- CI Gate 7 with 19 tests passing
- ManualCompositionE2ETest 5/5, AiGenerationE2ETest 7/7, GoogleAuthE2ETest 4/4, GenerateMeditationE2ETest 3/3

---

## AUTH-T013  Frontend E2E: Playwright full flow

**Type**: Task · **Layer**: Frontend E2E · **Parallelizable**: Yes
**Status**: Completed

**Description**
Cover all 5 BDD scenarios with mocked Google: new login  empty library; returning login  library with meditations; access without session  /login; logout; Google flow cancellation.

**Artifacts**
- rontend/tests/e2e/

**Acceptance Criteria**
- All Playwright tests pass in Chromium
- 
pm run test:e2e with no failures

---

## AUTH-T014  CI/CD: Pipeline updated with identity

**Type**: Task · **Layer**: CI/CD
**Status**: Completed

**Description**
Update .github/workflows/backend-ci.yml (add GOOGLE_CLIENT_ID, include BDD tag @identity in gate 1) and .github/workflows/frontend-ci.yml (add VITE_GOOGLE_CLIENT_ID, remove VITE_USER_ID).

**Acceptance Criteria**
- All gates of both CI pipelines pass
- No hardcoded userId in workflows
